#include <stack>


using std::stack;
using std::pair;
using std::make_pair;

int main(int argc, char** argv)
{


}



void bivariate_resultant(mpz_poly* f, mpzpoly* g, mpzpoly out)
{
	// construct Sylvester matrix

	
	// compute determinant
	stack< pair<int,int> > node;
	stack<int> cols;
	
	node.push(make_pair(0,cols.push((1<<n)-1)));

	while(!node.empty()) {
			
	}
}
